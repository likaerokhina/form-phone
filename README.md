# Phone Input Component

React компонент для ввода телефонных номеров с поддержкой масок, навигации с клавиатуры и выбора страны.

## Установка

```bash
yarn add form-phone
# или
npm install form-phone
```

## Использование

```tsx
import { PhoneInput } from "form-phone";
import "form-phone/style"; // опционально для базовых стилей

const masks = [
  {
    key: "ru",
    name: "Россия",
    emoji: "🇷🇺",
    prefix: "+7",
    mask: "(***) - *** - ** - **",
  },
  {
    key: "us",
    name: "США",
    emoji: "🇺🇸",
    prefix: "+1",
    mask: "(***) *** - ****",
  },
];

function App() {
  const [value, setValue] = useState("");

  return (
    <PhoneInput
      masks={masks}
      value={value}
      onChange={setValue}
    />
  );
}
```

## API

### PhoneInput Props

| Prop | Type | Описание |
|------|------|----------|
| `masks` | `MaskConfig[]` | Массив конфигураций масок |
| `value` | `string` | Значение телефона (может быть форматированным) |
| `onChange` | `(value: string) => void` | Callback при изменении значения |

### MaskConfig

```typescript
type MaskConfig = {
  key: string;        // Уникальный ключ маски
  name: string;       // Название страны
  emoji: string;      // Эмодзи флага
  prefix: string;     // Префикс номера (например, "+7")
  mask: string;       // Маска (например, "(***) - *** - ** - **")
};
```

## Возможности

- ✅ Каждый символ - отдельный input
- ✅ Навигация: Tab, стрелки влево/вправо, Backspace
- ✅ Валидация по Enter
- ✅ Выбор страны через dropdown с флагом
- ✅ Автоматическое форматирование
- ✅ Поддержка различных масок

## Первоначальная настройка

Перед началом работы с проектом ознакомьтесь с [инструкцией по настройке репозитория](SETUP.md).

## Разработка

```bash
# Установка зависимостей
yarn install

# Запуск dev-сервера
yarn dev
# или с кастомным портом
PORT=3001 yarn dev

# Запуск тестов
yarn test

# Проверка типов
yarn type-check

# Запуск Storybook
yarn storybook

# Сборка
yarn build
```

## Технологии

- React 18+
- TypeScript
- MobX
- Webpack 5
- Jest + React Testing Library
- Storybook

## Лицензия

MIT

